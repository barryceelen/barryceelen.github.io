# 2014 Portfolio

https://barryceelen.com

## Local development

Dependencies are managed via npm:

`npm install`

Assets and images are built via Grunt. Run the default task to start a local server and watch task:

`grunt`

To compile assets for deployment to the production site, run the 'build' task:

`grunt build`
